# 国创睿峰智能图书馆管理系统 - 业务流程图

## 1. 图书借阅完整流程

```mermaid
flowchart TD
    Start([开始借阅]) --> A{读者身份验证}
    A -->|刷读者证| B[系统读取读者信息]
    A -->|人脸识别| B
    A -->|验证失败| Error1[提示身份验证失败]
    Error1 --> End1([结束])

    B --> C{检查借阅资格}
    C -->|有逾期未还| D[显示逾期信息]
    D --> E{是否立即归还}
    E -->|是| Return[转入归还流程]
    E -->|否| End2([暂停借阅])

    C -->|达到借阅上限| F[提示达到借阅上限]
    F --> End3([结束])

    C -->|资格正常| G[扫描图书条码]
    G --> H{检查图书状态}
    H -->|图书不存在| I[提示图书未编目]
    I --> End4([结束])

    H -->|已被借出| J[提示图书已借出]
    J --> K{是否预约}
    K -->|是| Reserve[加入预约队列]
    K -->|否| End5([结束])

    H -->|图书在架| L[显示图书信息]
    L --> M{确认借阅}
    M -->|否| End6([取消借阅])
    M -->|是| N[创建借阅记录]

    N --> O[更新图书状态为借出]
    O --> P[计算应还日期]
    P --> Q[生成借阅凭证]
    Q --> R[发送借阅成功通知]

    R --> S{是否继续借阅}
    S -->|是| G
    S -->|否| T[打印借阅清单]
    T --> U[更新读者借阅统计]
    U --> V[记录操作日志]
    V --> W[AI系统更新用户画像]
    W --> End7([借阅完成])
```

## 2. 图书归还完整流程

```mermaid
flowchart TD
    Start([开始归还]) --> A[扫描图书条码]
    A --> B{检查图书状态}
    B -->|图书未借出| C[提示图书未借出]
    C --> End1([结束])

    B -->|找到借阅记录| D[显示借阅信息]
    D --> E{检查是否逾期}
    E -->|已逾期| F[计算逾期天数]
    F --> G[计算逾期费用]
    G --> H{是否缴纳罚款}
    H -->|否| I[记录欠费]
    H -->|是| J[处理罚款缴纳]

    E -->|未逾期| K[正常归还]
    I --> K
    J --> K

    K --> L[更新借阅记录状态]
    L --> M[更新图书状态为在架]
    M --> N{检查预约队列}

    N -->|有预约| O[分配给第一位预约者]
    O --> P[发送预约到书通知]
    P --> Q[设置预约保留期限]

    N -->|无预约| R[图书上架]
    Q --> S[更新流通记录]
    R --> S

    S --> T[更新读者借阅统计]
    T --> U[计算借阅积分]
    U --> V[AI系统记录阅读行为]
    V --> W[更新推荐模型数据]

    W --> X{是否继续归还}
    X -->|是| A
    X -->|否| Y[打印归还凭证]
    Y --> Z[发送归还成功通知]
    Z --> End2([归还完成])
```

## 3. 图书编目与典藏流程

```mermaid
flowchart TD
    Start([开始编目]) --> A[选择编目方式]
    A --> B{编目方式}

    B -->|智能编目| C[扫描ISBN]
    C --> D[云端检索300万+图书数据]
    D --> E{是否找到}
    E -->|找到| F[自动填充MARC数据]
    E -->|未找到| Manual

    B -->|批量导入| G[上传Excel/MARC文件]
    G --> H[解析文件数据]
    H --> I{数据验证}
    I -->|验证失败| J[显示错误信息]
    J --> K[修正数据]
    K --> I
    I -->|验证通过| L[批量创建编目记录]

    B -->|手工编目| Manual[手动输入图书信息]
    Manual --> M[填写书名/作者/出版社等]

    F --> N[AI智能分类]
    L --> N
    M --> N

    N --> O[自动生成中图分类号]
    O --> P[AI生成关键词和标签]
    P --> Q{防重复检查}
    Q -->|存在重复| R[提示已编目]
    R --> End1([结束])

    Q -->|无重复| S[保存编目信息]
    S --> T[进入典藏流程]

    T --> U[自动生成索书号]
    U --> V[生成条码号]
    V --> W[设置馆藏地]
    W --> X[设置副本数量]
    X --> Y[创建典藏记录]

    Y --> Z[打印条码标签]
    Z --> AA[打印索书号标签]
    AA --> AB[图书贴标]
    AB --> AC[图书上架]

    AC --> AD[更新馆藏统计]
    AD --> AE[同步到云端数据库]
    AE --> AF[AI系统更新图书特征]
    AF --> End2([编目典藏完成])
```

## 4. 读者证办理流程

```mermaid
flowchart TD
    Start([开始办证]) --> A[选择读者类型]
    A --> B{读者类型}

    B -->|学生| C[输入学生信息]
    C --> D[填写姓名/班级/年级]

    B -->|教师| E[输入教师信息]
    E --> F[填写姓名/部门/工号]

    D --> G[设置借阅权限]
    F --> G

    G --> H[学生:最多5本/次,30天]
    G --> I[教师:最多10本/次,60天]

    H --> J[拍摄人脸照片]
    I --> J

    J --> K[人脸特征提取]
    K --> L{特征质量检查}
    L -->|质量不合格| M[重新拍摄]
    M --> J

    L -->|质量合格| N[保存人脸特征]
    N --> O[生成读者证号]
    O --> P{是否需要实体卡}

    P -->|需要| Q[打印读者证]
    Q --> R[覆膜处理]

    P -->|不需要| S[启用刷脸借书]
    R --> S

    S --> T[创建读者账号]
    T --> U[设置初始密码]
    U --> V[绑定微信账号]

    V --> W[发送办证成功通知]
    W --> X[同步到一卡通系统]
    X --> Y[AI系统创建用户画像]

    Y --> Z{批量办证}
    Z -->|是| AA[导入Excel名单]
    AA --> AB[批量创建读者]
    AB --> End1([批量办证完成])

    Z -->|否| End2([单个办证完成])
```

## 5. 图书盘点流程

```mermaid
flowchart TD
    Start([开始盘点]) --> A[制定盘点计划]
    A --> B[选择盘点范围]
    B --> C{盘点方式}

    C -->|分类盘点| D[按中图分类法选择类别]
    D --> E[生成该类别图书清单]

    C -->|全馆盘点| F[生成全部图书清单]

    C -->|指定区域| G[选择馆藏地]
    G --> H[生成区域图书清单]

    E --> I[打印盘点清单]
    F --> I
    H --> I

    I --> J{盘点设备}
    J -->|RFID盘点| K[使用RFID扫描仪]
    K --> L[批量扫描书架]
    L --> M[自动识别图书标签]

    J -->|条码扫描| N[使用条码扫描枪]
    N --> O[逐本扫描图书]

    M --> P[记录扫描结果]
    O --> P

    P --> Q{是否继续扫描}
    Q -->|是| R[扫描下一批次]
    R --> J

    Q -->|否| S[生成盘点报告]
    S --> T[对比系统数据]

    T --> U{盘点差异分析}
    U --> V[盘亏图书列表]
    U --> W[盘盈图书列表]
    U --> X[错架图书列表]

    V --> Y[标记丢失状态]
    W --> Z[核实新增图书]
    X --> AA[生成调架任务]

    Y --> AB[AI分析丢失原因]
    Z --> AC[更新馆藏记录]
    AA --> AD[打印调架清单]

    AB --> AE[生成盘点总结]
    AC --> AE
    AD --> AE

    AE --> AF[更新库存数据]
    AF --> AG[发送盘点报告]
    AG --> AH[记录盘点日志]
    AH --> End([盘点完成])
```

## 6. 图书荐购与捐赠流程

```mermaid
flowchart TD
    Start([开始]) --> A{选择类型}

    A -->|荐购| B[读者发起荐购]
    B --> C[微信扫ISBN或输入书名]
    C --> D[系统查询图书信息]
    D --> E{是否已有馆藏}
    E -->|已有| F[提示已有馆藏]
    F --> End1([结束])

    E -->|没有| G[填写荐购理由]
    G --> H[提交荐购申请]
    H --> I[系统记录荐购信息]
    I --> J[AI分析荐购价值]

    J --> K[生成采购建议分数]
    K --> L{管理员审核}
    L -->|拒绝| M[发送拒绝通知]
    M --> N[说明拒绝原因]
    N --> End2([荐购失败])

    L -->|通过| O[加入采购清单]
    O --> P[批量采购处理]
    P --> Q[图书到馆]
    Q --> R[通知荐购者]
    R --> S[优先预约权]

    A -->|捐赠| T[读者发起捐赠]
    T --> U[微信扫描ISBN]
    U --> V[自动获取图书信息]
    V --> W{检查图书适用性}

    W -->|不适合| X[提示不接受该类图书]
    X --> End3([捐赠失败])

    W -->|适合| Y[生成捐赠单]
    Y --> Z[选择捐赠方式]
    Z --> AA{捐赠方式}

    AA -->|快递| AB[生成快递单号]
    AB --> AC[等待图书寄到]

    AA -->|现场| AD[到馆捐赠]

    AC --> AE[管理员验收]
    AD --> AE

    AE --> AF{图书质量检查}
    AF -->|不合格| AG[退还给捐赠者]
    AG --> End4([捐赠失败])

    AF -->|合格| AH[登记捐赠信息]
    AH --> AI[生成捐赠证书]
    AI --> AJ[图书编目入库]

    S --> AK[更新推荐模型]
    AJ --> AK
    AK --> AL[记录贡献积分]
    AL --> End5([流程完成])
```

## 7. AI推荐生成流程

```mermaid
flowchart TD
    Start([开始推荐]) --> A[触发推荐请求]
    A --> B{推荐场景}

    B -->|首页推荐| C[获取用户ID]
    B -->|详情页推荐| D[获取当前图书ID]
    B -->|搜索推荐| E[获取搜索关键词]

    C --> F[加载用户画像]
    D --> F
    E --> F

    F --> G[提取用户特征]
    G --> H[历史借阅记录]
    G --> I[浏览行为数据]
    G --> J[用户基础属性]

    H --> K[多路召回策略]
    I --> K
    J --> K

    K --> L[协同过滤召回]
    K --> M[内容相似召回]
    K --> N[热门图书召回]
    K --> O[新书推荐召回]
    K --> P[主题推荐召回]

    L --> Q[候选集合并]
    M --> Q
    N --> Q
    O --> Q
    P --> Q

    Q --> R[特征工程]
    R --> S[用户特征x图书特征x上下文特征]
    S --> T[Wide&Deep模型预测]

    T --> U[预测借阅概率]
    U --> V[按分数排序]
    V --> W{业务规则过滤}

    W --> X[过滤已借图书]
    W --> Y[过滤不适龄图书]
    W --> Z[过滤库存为0]

    X --> AA[多样性优化]
    Y --> AA
    Z --> AA

    AA --> AB[避免同类图书扎堆]
    AB --> AC[保证作者多样性]
    AC --> AD[平衡新旧图书比例]

    AD --> AE[生成TOP-N推荐列表]
    AE --> AF[缓存推荐结果]
    AF --> AG[记录推荐日志]

    AG --> AH{用户反馈}
    AH -->|点击| AI[记录点击行为]
    AH -->|借阅| AJ[记录借阅转化]
    AH -->|忽略| AK[记录曝光未点击]

    AI --> AL[更新推荐模型]
    AJ --> AL
    AK --> AL

    AL --> AM[计算推荐指标]
    AM --> AN[CTR/转化率/满意度]
    AN --> End([推荐完成])
```

## 8. 智能问答处理流程

```mermaid
flowchart TD
    Start([用户提问]) --> A[接收用户输入]
    A --> B{输入类型}

    B -->|文字| C[文本预处理]
    B -->|语音| D[语音识别ASR]
    D --> E[转换为文本]
    E --> C

    C --> F[分词处理]
    F --> G[BERT意图识别]

    G --> H{意图分类}

    H -->|图书查询| I[NER实体抽取]
    I --> J[提取书名/作者/ISBN]
    J --> K[ES全文检索]
    K --> L[返回图书信息]

    H -->|借阅咨询| M[匹配业务规则库]
    M --> N[提取规则信息]
    N --> O[生成规则说明]

    H -->|操作指导| P[检索FAQ知识库]
    P --> Q[语义向量匹配]
    Q --> R{相似度判断}
    R -->|高相似度>0.7| S[返回FAQ答案]
    R -->|低相似度<0.7| T[触发人工客服]

    H -->|推荐请求| U[调用推荐引擎]
    U --> V[获取推荐列表]
    V --> W[格式化推荐结果]

    H -->|数据查询| X[查询用户数据]
    X --> Y[借阅记录/预约信息]
    Y --> Z[数据脱敏处理]

    L --> AA{需要多轮对话?}
    O --> AA
    S --> AA
    W --> AA
    Z --> AA
    T --> Human[人工介入]

    AA -->|是| AB[保存对话上下文]
    AB --> AC[等待用户追问]
    AC --> AD[结合上下文理解]
    AD --> G

    AA -->|否| AE[生成最终回复]
    Human --> AE

    AE --> AF{输出方式}
    AF -->|文字| AG[返回文本回复]
    AF -->|语音| AH[TTS语音合成]
    AH --> AI[播放语音回复]

    AG --> AJ[记录对话日志]
    AI --> AJ

    AJ --> AK[用户满意度评价]
    AK --> AL{满意度}
    AL -->|满意| AM[记录成功案例]
    AL -->|不满意| AN[标记优化点]

    AM --> AO[更新知识库]
    AN --> AO
    AO --> AP[优化NLP模型]
    AP --> End([对话结束])
```

## 流程图说明

### 1. 图书借阅流程

- **核心节点**：身份验证、资格检查、图书状态检查、借阅确认
- **异常处理**：逾期处理、借阅上限、图书不可借
- **智能特性**：人脸识别、AI更新用户画像

### 2. 图书归还流程

- **核心节点**：图书验证、逾期检查、预约处理、积分计算
- **异常处理**：逾期罚款、图书未借出
- **智能特性**：AI记录阅读行为、更新推荐模型

### 3. 图书编目与典藏流程

- **核心节点**：多种编目方式、防重复检查、自动生成编号
- **智能特性**：云端数据检索、AI智能分类、自动生成标签

### 4. 读者证办理流程

- **核心节点**：读者类型选择、权限设置、人脸采集
- **异常处理**：人脸质量检查、重新拍摄
- **智能特性**：人脸特征提取、刷脸借书

### 5. 图书盘点流程

- **核心节点**：盘点计划、扫描方式、差异分析、结果处理
- **智能特性**：RFID批量扫描、AI分析丢失原因

### 6. 图书荐购与捐赠流程

- **核心节点**：申请提交、审核流程、采购/验收、通知反馈
- **智能特性**：AI分析荐购价值、自动获取图书信息

### 7. AI推荐生成流程

- **核心节点**：用户画像、多路召回、模型预测、业务过滤
- **智能特性**：Wide&Deep模型、多样性优化、实时反馈更新

### 8. 智能问答处理流程

- **核心节点**：意图识别、实体抽取、知识检索、回复生成
- **智能特性**：BERT模型、语音识别/合成、多轮对话
