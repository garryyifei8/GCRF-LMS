spring:
  application:
    name: auth-service
  # Disable Nacos discovery/config during integration tests (Testcontainers-based)
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
