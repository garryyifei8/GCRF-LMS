spring:
  application:
    name: system-service
  # Disable Nacos discovery/config during integration tests (Testcontainers-based)
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
